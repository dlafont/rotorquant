# Codex Chat Copies

This folder stores copied Codex session transcripts for `PRJ-rotorquant`.

## Naming

Use a descriptive filename that matches the session topic or task.

Example:

- `Clone_and_compile_rotoquant_project_chat.md`

## Purpose

These copies exist so a later session can recover the previous context if the
live thread is corrupted, disconnected, or otherwise unavailable.
